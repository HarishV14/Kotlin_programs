fun main() {
    // Declare the radius as a Double
    val radius: Double = 5.0

    // Calculate the area using the formula: Area = π * r^2
    val area: Double = Math.PI * radius * radius

    // Print the result
    println("Radius of the circle: $radius")
    println("Area of the circle: $area")
}
